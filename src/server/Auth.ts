import { Packet, AuthPacket } from '../common/Packet';

export function login(packet: AuthPacket) {
    if (packet.username === 'tim' && packet.password === 'asd') {

    }
}

export function create() {

}
