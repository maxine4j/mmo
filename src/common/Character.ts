export default class Character {
    public name: string;

    public level: number;

    public constructor(name: string, level: number) {
        this.name = name;
        this.level = level;
    }
}
